# docker build -f Dockerfile.node.linux -t paulgilchrist/ballistics-node-linux .
# docker run -d -p 80:3000 paulgilchrist/ballistics-node-linux
# docker inspect --format="http://{{.NetworkSettings.Networks.nat.IPAddress}}:3000" <containerId>
# Test on http://<containerIP>:3000
# docker rm -f <containerID>
FROM node:alpine
RUN mkdir -p /usr/src/app
COPY dist /usr/src/app
WORKDIR /usr/src/app
# RUN npm install
EXPOSE 3000
CMD [ "node", "server.js" ]
# docker push paulgilchrist/ballistics-node-linux